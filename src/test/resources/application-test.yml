spring:
  application.name: boot-rest-test
  main.show-banner: false
  data.elasticsearch:
    cluster-name: boot-rest-test
    properties:
      node.name: Boot Rest Test

server.port: 0
management.port: 0

logging.level.root: WARN
