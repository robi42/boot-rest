spring:
  application.name: boot-rest-test
  main.show-banner: false
  data.elasticsearch.properties:
    node.name: BootRestTestEs
    index.store.type: memory

server.port: 0
management.port: 0

logging.level.root: WARN
